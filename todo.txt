Start()
// On set up tout ce qui est relatif à l'instance SEULEMENT

SetInterinstance ()
// On setup les relations entre instances
// event + get component

2e healer :
	boost();

prefab =/= scriptable object ?